spring:
  application:
    name: service-product

  # 告诉应用nacos注册中心在哪
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos

server:
  port: 8000